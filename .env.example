# Copy this file to .env and fill in real values for running testnet integration tests
# Keep the real .env out of version control (.gitignore already contains .env)

# RPC URL for the Sepolia (or other) testnet node
TESTNET_RPC_URL=

# BIP-39 mnemonic used for test accounts (ensure this is funded on the testnet)
TESTNET_SEED_PHRASE=

# ERC-20 token address to test against (WETH on Sepolia in earlier runs)
TEST_TOKEN_ADDRESS=

# Receiver address used by ETH transfer test
RECEIVER=

# Optional test tuning
TEST_CONFIRMATIONS=2
TEST_MAX_RETRIES=3
TEST_RETRY_DELAY=5000
TEST_TIMEOUT=180000